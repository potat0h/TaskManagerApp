<Page
    x:Class="TaskManagerApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TaskManagerApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    NavigationCacheMode="Required"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <!-- Navigation Bar -->
        <StackPanel HorizontalAlignment="Center" Margin="-1386,0,0,969" Orientation="Horizontal">
            <Button Content="ADD" Click="AddTaskButton_Click"/>
            <Button Content="DELETE" Click="DeleteTaskButton_Click"/>
        </StackPanel>

        <!-- Task List -->
        <ListView x:Name="TaskListView" Margin="20">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Task}" VerticalAlignment="Center"/>
                        <Button Content="Mark as Complete" Click="MarkAsComplete_Click"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
